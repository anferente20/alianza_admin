name: alianza_admin
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  either_dart: ^1.0.0
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  go_router: ^10.0.0
  provider: ^6.0.5
  riverpod: ^2.3.7

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/imgs/
